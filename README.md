# NetSniffer

一个基于 Electron + React + TypeScript + Zustand 的现代化网络抓包工具，支持 HTTP/HTTPS 请求拦截和分析。

## 功能特性

- 📊 **实时抓包**: 集成Whistle，实时捕获和分析网络请求
- 🔒 **HTTPS解密**: 自动生成和安装CA证书，支持HTTPS请求解密
- 💾 **数据导出**: 支持JSON和CSV格式导出
- 🖥️ **跨平台**: 支持macOS和Windows

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式运行

```bash
npm run dev
```

### 生产模式运行

```bash
npm start
```

### 构建与打包

```bash
# 编译代码（不生成安装包）
npm run build

# 生成安装包（按平台）
npm run dist:mac    # macOS
npm run dist:win    # Windows
npm run dist:linux  # Linux

# 多平台一次性
npm run dist        # macOS + Windows（参见 package.json）
```

详细的构建指南请参考 [构建指南](docs/BUILD_GUIDE.md)。

## 使用说明

1. **启动应用**: 运行应用后进入抓包主界面
2. **配置证书**: 在"证书管理"中安装并信任根证书
3. **开始抓包**: 在"抓包分析"中查看实时请求
4. **设置代理**: 根据需要配置系统代理或浏览器代理

## 技术栈

- **主进程**: Electron + Node.js + TypeScript
- **渲染进程**: React 18 + Ant Design 5 + TypeScript
- **状态管理**: Zustand
- **路由**: React Router
- **代理服务**: Whistle
- **构建工具**: Webpack 5 + electron-builder

## 项目结构

```
netsniffer/
├── src/
│   ├── main/              # Electron 主进程（TypeScript）
│   │   ├── api/           # IPC API 注册
│   │   ├── core/          # 业务核心（证书、代理）
│   │   ├── ipc-handlers/  # IPC 处理器
│   │   ├── preload.ts     # 预加载脚本
│   │   └── index.ts       # 主入口
│   ├── renderer/          # 渲染进程（React + TypeScript）
│   │   ├── components/    # 通用组件（侧边栏、对话框等）
│   │   ├── extensions/    # 功能面板（抓包、证书等）
│   │   ├── store/         # 状态管理（Zustand）
│   │   ├── styles/        # 样式文件
│   │   └── index.tsx      # 渲染入口
│   ├── apis/              # API 请求层
│   └── shared/            # 共享工具和类型
├── app/                   # 构建产物（Electron 启动目录）
├── certs/                 # 证书文件
├── .whistle/              # Whistle 配置
└── docs/                  # 文档（见 docs/README.md 索引）
```

## 开发指南

详细的开发文档请参考 `docs/` 目录：

- [开发指南](docs/DEVELOPMENT.md)
- [API文档](docs/API.md)
- [故障排除](docs/TROUBLESHOOTING.md)
- [部署指南](docs/DEPLOYMENT.md)
